# Work Habits

## Getting Context

Before reading source code, always check openspec for existing design decisions:

```bash
openspec list --json
```

Then read the relevant change's `proposal.md`, `design.md`, and `specs/` to understand the why and how before touching code.
